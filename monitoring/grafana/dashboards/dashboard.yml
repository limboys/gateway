# dashboard.yml - Grafana Dashboard 自动加载配置

apiVersion: 1

providers:
  - name: 'API Proxy Dashboards'
    orgId: 1
    folder: 'API Proxy'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
